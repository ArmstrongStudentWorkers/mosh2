<%= simple_form_for(@term) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, placeholder: 'e.g. Summer' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
