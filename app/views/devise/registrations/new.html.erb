<h2>TechFest Student Sign-Up</h2>

<p class="text-center">This form is for student sign-up only. Student accounts will only be accessible during the week before TechFest.<br>Faculty and staff, please check your @armstrong.edu email,<br> or go <%= link_to 'HERE', new_user_password_path %> to get your permanent login information.</p><br><br>

<div class="row">
  <div class="span8 offset2">
    <%= simple_form_for(resource, :as => resource_name, html: { class: 'form-horizontal' }, :url => registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :name, :required => true, :autofocus => true %>
        <%= f.input :email, :required => true %>
        <%= f.input :password, :required => true %>
        <%= f.input :password_confirmation, :required => true %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Sign up", class: 'btn btn-primary' %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
